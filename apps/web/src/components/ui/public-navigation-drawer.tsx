"use client";

// Kept as a compatibility export for older imports. The active public
// navigation drawer now lives in components/navigation/mobile-navigation-drawer.
export function PublicNavigationDrawer() {
  return null;
}
